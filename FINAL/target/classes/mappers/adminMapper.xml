<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.auction.mapper.AdminMapper">
	<!-- member -->
	<select id="showMember" resultType="com.auction.vo.MemberVo">
		SELECT * FROM member order by grade asc;
	</select>
	<select id="buyItem" resultType="com.auction.vo.AuctionVo">
		SELECT * FROM auction where ID=#{ID} order by buydate desc;
	</select>
	<select id="saleItem" resultType="com.auction.vo.ProductVo">
		select * from product where ID=#{ID} order by uploaddate desc;
	</select>


	<!-- product -->
	<select id="dealProduct" resultType="com.auction.vo.ProductVo">
		select p.*, grade from product p left outer join member m on m.ID=p.ID where admin=#{admin} and deal=#{deal} order by uploaddate desc, grade asc;
	</select>
	<select id="adminProduct" resultType="com.auction.vo.ProductVo">
		select p.*, grade from product p left outer join member m on m.ID=p.ID where admin=#{admin} order by uploaddate desc, grade asc;
	</select>
	
	<select id="showProduct" resultType="com.auction.vo.ProductVo">
		SELECT p.*, grade FROM product p left outer join member m on m.ID=p.ID order by uploaddate desc, grade asc;
	</select>
	
	<update id="updateAdmin">
		update product set admin=#{admin}, deal=#{deal} where pno=#{pno};
	</update>
</mapper>