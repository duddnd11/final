<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.yg.mapper.ProductMapper">
	<insert id="insertProduct">
		INSERT INTO product(pname, image, filenames, price, uploaddate, deadlinedate, hitcount, ID, bestmoney, admin, category, auctionmenu, deal, moneyup) VALUES(#{pname},#{image},#{filenames},#{price},#{uploaddate},#{deadlinedate},#{hitcount},#{ID},#{bestmoney},#{admin},#{category},#{auctionmenu},#{deal},#{moneyup})
	</insert>
	
	<select id="selectAuction" resultType="com.auction.vo.ProductVo">
<<<<<<< HEAD
		select * from product where auctionmenu='일반' order by uploaddate desc;
=======
		select * from product where auctionmenu='일반' order by uploaddate desc;
>>>>>>> refs/remotes/origin/main
	</select>

	<select id="selectAuctionBlind" resultType="com.auction.vo.ProductVo">
		select * from product where auctionmenu='블라인드' order by uploaddate desc;
	</select>
		
	<select id="selectPop" resultType="com.auction.vo.ProductVo">
		SELECT * FROM product order by hitcount desc;
	</select>
<<<<<<< HEAD
	<select id="selectHurry" resultType="com.auction.vo.ProductVo">
=======

	<select id="selectHurry" resultType="com.auction.vo.ProductVo">
>>>>>>> refs/remotes/origin/main
		SELECT *  from product order by (TIMESTAMPDIFF(day, NOW(), deadlinedate)+1) asc;
	</select>
	
	<select id="selectOne" resultType="com.auction.vo.ProductVo">
		SELECT * FROM product where pno=#{pno}
	</select>
	<update id="hitcountUp">
	</update>
	
	<update id="timeout">
		update product set timeout=(TIMESTAMPDIFF(day, NOW(), deadlinedate)+1);
	</update>
	<update id="startmoney">
		update product set startmoney=price*0.6;
	</update>
	<update id="lastmoney">
		update product set lastmoney=price*1.4;
	</update>
	
</mapper>  